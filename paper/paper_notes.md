# Paper Notes

Planned manuscript files:

```text
A096242_v10.tex
A096242_v10.pdf
```

The paper should cite this raw package as a forthcoming GitHub repository:

```text
https://github.com/carcorti/A096242
```

The Zenodo DOI is not available yet and must remain an explicit placeholder
until Carlo supplies the final archived DOI.

The manuscript should not state that the large binary level files are
distributed through GitHub. They are generated artifacts; the public package
contains the source code, the extended b-file, validation reports, the
saved-level certifier, the D11 GMP re-enumerator, and
`levels_manifest.tsv`. It also contains a local Markdown mirror of the
Sinclair Miller-Rabin SPRP bases table cited by the paper.

Before final public release, replace the versioned manuscript name with the
unversioned final manuscript name after the GitHub tag/commit and Zenodo DOI
are available.

`A096242_v10` is the current manuscript candidate.  It contains the explicit
clarification that paths such as `levels/D10.bin` and `levels/D11.bin` refer
to generated local campaign artifacts documented by `levels_manifest.tsv`,
not to binary files included in the public repository.
